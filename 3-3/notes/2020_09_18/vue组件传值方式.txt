父组件->子组件
父组件使用子组件时使用一个自定义属性，子组件的props中该属性接收。

子组件->父组件
子组件$emit(事件名,参数)，父组件v-on:事件名

兄弟组件及不相关组件
使用一个vue实例bus作为中间层，bus.$emit(事件名,参数)传出，bus.$on(事件名,(参数)=>{})接收

另外还可使$children、$refs、$root、$parent访问其他组件，调用其方法，修改其数据，但这种方式不被推荐，滥用的话会导致数据管理混乱。
