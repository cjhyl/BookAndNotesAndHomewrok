二、js项目中的类型增强
1、类型补充声明：
在typescript项目中，那些.d.ts文件就是类型补充声明文件。
使用原因：引入第三方js模块的时候，因为没有类型说明，所以会导致无法使用，解决方案就是类型补充声明。

三、npm模块加载失败的原因解析。
某些模块因为需要下载2进制数据（为了兼容运行环境），这种行为没有考虑中国的网络跨越问题，所以很慢很卡容易失败，例如gulp-imagemin
可配置etc/hosts文件，可单独配置二进制模块的npm镜像。
